[mypy]
exclude = sqlelf/_vendor
# Check against the oldest Python we support rather than whichever interpreter
# happens to be running mypy, so the answer is the same everywhere. pyright's
# equivalent is pythonVersion in pyproject.toml.
python_version = 3.11

[mypy-sqlelf._vendor.*]
follow_imports = skip